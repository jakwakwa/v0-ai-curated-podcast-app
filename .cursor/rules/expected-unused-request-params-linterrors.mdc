---
alwaysApply: true
---
# Cursor agents fixing linting rules

When running in into a "'request' is declared but its value is never...
"

```typescript
export async function GET(_request: Request) {}
```

CRITICAL: This rule deals explicity with the "request" params edge case. In general no unused variables should be in our code at all, unless there's a really good explanation to use it.
