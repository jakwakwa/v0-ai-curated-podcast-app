---
alwaysApply: true
---
# Logical expression shouldnt contain unnecessary complexity

1. use Simplified Logic Expression

DO:

`if (!(config.hasHost && config.hasFrom && config.hasUser && config.hasPass))`

DONT
`if (!(config.hasHost && config.hasFrom ) || !config.hasUser || !config.hasPass) {`
