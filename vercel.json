{
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install --frozen-lockfile --ignore-scripts --shamefully-hoist",
  "framework": "nextjs",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}
